function where(arr, num) {
  var arrSorted = arr.sort(function(x,y){
    return x-y;
  });
  var place = 0;
  for(i=0; i<arr.length; i++){
    if(arr[i] < num){
      place++;
    } else {
      break;
    }
  }
  return place;
}

where([5, 3, 20, 3], 5);
